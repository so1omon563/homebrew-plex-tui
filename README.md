# homebrew-plex-tui

Homebrew tap for [plex-tui](https://github.com/so1omon563/plex-tui).

```bash
brew tap so1omon563/plex-tui
brew install plex-tui
```

`plex-tui` depends on `mpv` for playback; the formula installs that dependency
through Homebrew.
